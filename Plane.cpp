#include "Plane.h"

int Plane::nextID = 1;

// Constructor
Plane::Plane()
    : ID(nextID++){
    // Constructor body (if needed)
}

// Destructor
Plane::~Plane() {
    // Destructor implementation (if needed)
}